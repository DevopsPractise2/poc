pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
               sh '''mvn clean install dockerfile:build'''
            }
        }
        stage('Image check') { 
            steps {
                sh '''docker images '''
            }
        }
        stage('Deploy') { 
            steps {
                // 
            }
        }
    }
}
