pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                mvn clean install dockerfile:build 
            }
        }
        stage('Image check') { 
            steps {
                docker images 
            }
        }
        stage('Deploy') { 
            steps {
                // 
            }
        }
    }
}
